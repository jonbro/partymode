# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: partymode
  host: localhost
  username: root
  password: root
production:
  adapter: mysql
  database: partymode
  host: localhost
  username: root
  password: root
